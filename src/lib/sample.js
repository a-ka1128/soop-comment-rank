/**
 * "예시 글" 버튼과 API 헬스체크가 함께 쓰는 표본.
 *
 * 분류가 또렷하게 잡히는 글이라 감지 로직 확인용으로도 쓴다.
 * 이 글이 삭제되면 헬스체크가 실패하면서 알려 주니, 그때 다른 글로 바꾸면 된다.
 */
export const SAMPLE_POST = {
  bjId: 'ecvhao',
  postNo: '203249055',
  url: 'https://www.sooplive.com/station/ecvhao/post/203249055',
}
